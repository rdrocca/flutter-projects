import 'package:flutter/material.dart';

import './home_simple_music_animation.dart';

class MainSimpleMusicAnimation extends StatelessWidget {
  const MainSimpleMusicAnimation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(title: Text("Simple Music Animation")),
      body: HomeSimpleMusicAnimation(),
    );
  }
}
